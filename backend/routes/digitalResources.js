// routes/digitalResources.js
const express = require('express');
const router = express.Router();

// You'll add specific digital resource routes here later
// For now, it just needs to export a router.

module.exports = router;